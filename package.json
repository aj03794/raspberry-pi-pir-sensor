{
    "name": "raspberry-pi-pir-sensor",
    "version": "1.0.0",
    "description": "",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "build": "rm -rf dist && babel src --out-dir dist",
        "start": "npm run build && sudo node dist/index.js",
        "start-dev": "npm run build && node dist/index.js",
        "postinstall": "npmpd"
    },
    "keywords": [],
    "author": "Adam Johnston",
    "license": "ISC",
    "devDependencies": {
        "babel-cli": "^6.26.0",
        "babel-preset-env": "^1.6.1"
    },
    "babel": {
        "presets": [
            "env"
        ]
    },
    "dependencies": {
        "npm-platform-dependencies": "^0.1.0",
        "redis": "^2.8.0"
    },
    "linuxDependencies": {
        "johnny-five": "^0.14.3",
        "raspi-io": "^9.0.0"
    }
}
